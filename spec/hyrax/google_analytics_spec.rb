require "spec_helper"

describe Hyrax::GoogleAnalytics do
  it "has a version number" do
    expect(Hyrax::GoogleAnalytics::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
